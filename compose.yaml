services:
  youtube-media:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: youtube-media
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    volumes:
      - ./storage:/app/storage
